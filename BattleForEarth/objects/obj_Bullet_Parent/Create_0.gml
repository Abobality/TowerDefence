var nearest = instance_nearest(path_get_point_x(pth_level_1,10),path_get_point_y(pth_level_1,10),obj_Parent_Zombie)

move_towards_point(nearest.x,nearest.y,5)

maxlifetime = 10
bulletPersistent = 1;

image_speed = 0;

image_index = -1;